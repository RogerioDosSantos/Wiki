
services:
  mkdocs:
    build: 
      context: .
      dockerfile: build.docker
    container_name: mkdocs-mermaid
    ports:
      - "8001:8000"
    # volumes:
    #   - ../docs/src:/docs/docs
    #   - ./mkdocs.yml:/docs/mkdocs.yml
